# Looks up a person's phone number and address from public directories.
# Input format: "First Last" — split and queried against a people-search source.


def lookup_fullname(full_name: str) -> str:
    # Phase 4: parse first/last name, query Pages Blanches or equivalent directory
    raise NotImplementedError("Full name lookup not yet implemented")
