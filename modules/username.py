# Checks whether a username exists across multiple social platforms.
# Probes each platform URL and interprets HTTP 200 as "exists", 404 as "not found".


def lookup_username(username: str) -> str:
    # Phase 3: strip leading @, probe GitHub/Instagram/Twitter/Reddit/TikTok/LinkedIn
    raise NotImplementedError("Username lookup not yet implemented")
